name: test

push:
    branches: [ main ]

on:
  pull_request:
    branches: [ main ]

jobs:
  fix-code-style:
    if: '! github.event.pull_request.draft'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'test action'
